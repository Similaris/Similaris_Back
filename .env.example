# Rodando local sem Docker (SQLite):
DATABASE_URL=sqlite:///./similaris.db

# Rodando contra o PostgreSQL do Docker (docker compose up db):
# DATABASE_URL=postgresql+psycopg2://similaris:similaris@localhost:5432/similaris

SECRET_KEY=similaris-dev-secret-troque-em-producao
ACCESS_TOKEN_EXPIRE_MINUTES=480
